globals:
  seed: 44

env:
  name: "gripper"
  normalizer_type: "off"
  env_kwargs: 
    object_qty: 4
    object_names: ['ball', 'box', 'desk', 'hammer']

trainer_args:
  trans_optimization_epochs: 50

replay_buffer:
  common:
    max_transitions: 1000000

skill:
  name: "metra"
  discrete: false
  dim_option: 2