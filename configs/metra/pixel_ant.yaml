globals:
  encoder: 'cnn'

env:
  name: "dmc_quadruped"
  normalizer_type: "squashed"
  frame_stack: 3
  env_kwargs: null

trainer_args:
  trans_optimization_epochs: 200

replay_buffer:
  common:
    max_transitions: 300000

skill:
  name: "metra"
  discrete: false
  dim_option: 4