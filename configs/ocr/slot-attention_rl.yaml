defaults:
  - slot-attention

optimizer:
  encoder_optimizer:
    lr_scheduler:
      warmup_steps: 0
      decay_steps: 1
      decay_rate: 1

  slot_optimizer:
    lr_scheduler:
      warmup_steps: 0
      decay_steps: 1
      decay_rate: 1
  
  decoder_optimizer:
    lr_scheduler:
      warmup_steps: 0
      decay_steps: 1
      decay_rate: 1